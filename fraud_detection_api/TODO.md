# TODO

- [ ] Fix preprocessing crash: ensure output directory exists before writing CSVs in `preprocess_data.py`.
- [ ] Run unit tests (`pytest`).
- [ ] Re-run `python preprocess_data.py` to confirm pipeline completes and files are written.

